diff --git a/git-foresta b/git-foresta
index da3b2c3..13a2e5d 100755
--- a/git-foresta
+++ b/git-foresta
@@ -78 +78 @@ my $Repo          = Git->repository($ENV{"GIT_DIR"} || ".");
-my $Pretty_fmt    = "format:%H\t%at\t%an\t%C(reset)%C(auto)%d%C(reset)\t%s";
+my $Pretty_fmt    = "format:%h\t\t\t%C(reset)%C(auto)%d%C(reset)\t%s";
@@ -83 +83 @@ my $Show_status   = 1;
-my $Style         = 1;
+my $Style         = 10;
@@ -88 +88 @@ my %Color         = (
-  "hash"         => "\e[0;35m",
+  "hash"         => "\e[1;30m",
@@ -90 +90 @@ my %Color         = (
-  "author"       => "\e[0;33m",
+  "author"       => "\e[1;30m",
@@ -113 +113 @@ my @Branch_colors_now     = ();
-my $Hash_min_width        = 8;
+my $Hash_min_width        = 0;
@@ -115,3 +115,3 @@ my $Hash_width            = 0; # zero means auto
-my $Date_format           = "%Y-%m-%d %H:%M";
-my $Date_width            = 16;
-my $Graph_margin_left     = 2;
+my $Date_format           = "";
+my $Date_width            = 0;
+my $Graph_margin_left     = 0;
